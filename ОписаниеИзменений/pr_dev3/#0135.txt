Подсистемы:
   игсНовыеОбъектыМетаданных - включены объекты: ОбщийМодуль.игсОборотныйДляБДР

ОбщиеМодули:
    игсОборотныйДляБДР:
        ТекстЗапроса() - возвращает текста запроса для отчета;
		КонстантныеПараметры() - возвращает предопределенные параметры для запроса.

ВнещниеОтчеты:
	игсОборотыДляБДР:
		Реквизиты (пока не используются, это заготовка для следующей задачи):
			ДатаНачала - тип Дата
			ДатаОкончания - тип Дата
			Организация - тип СправочникСсылка.Организация
			СписокСвертки - тип СписокЗначений
			СписокСчетов - тип СписокЗначений
			СписокПроектов - тип СписокЗначений
			ЭтоДоЗагрузкаРезерва - тип Булево
		Формы (пока не используются, это заготовка для следующей задачи):
			ФормаОтчета
			ФормаНастроек

		Макеты:
			ОсновнаяСхемаКомпоновкиДанных - основная СКД
			
		Модули:
			МодульОбъекта:
				ПриКомпоновкеРезультата() - процедура подтягивает необходимые данные из общего модуля для построения запроса и из формы отчета. Потом данные уходят набором в СКД;
				СведенияОВнешнейОбработке(), ПолучитьТаблицуКоманд() и ДобавитьКоманду() - комплекс программного интерфейса для подключения отчета в качестве дополнительного;
				ПолучитьИндексПараметра() - возвращает индекс запрашиваемого параметра.