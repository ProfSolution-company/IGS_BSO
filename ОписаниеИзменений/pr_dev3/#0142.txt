ВнешниеОбработки:
	игсПерераспределение46:
		Реквизиты:
			Дата - тип Дата без времени
			Организация - тип СправочникСсылка.Организации
			ОперацияБух - тип ДокументСсылка.ОперацияБух
		Формы:
			Форма - основная форма обработки:
				Вынесены основные реквизиты
				Вынесена команда "ВыполнитьДействие"
				Выведены вспомогательные надписи
		Модули:
			МодульОбъекта:
				СведенияОВнешнейОбработке() - функция позволяет загружать обработку в базу.
				СоздатьИзменитьОперацияБух() - функция создает/меняет проводки в новом/указанном документе.
				СформироватьПроводку() - создает проводку для указанного документа.
				ЗаписьПеременных() - данные из выборки записываются в переменные.
			МодульФормы "Форма":
				ПриОткрытии() - процедура заполняет Дату и Организацию на форме.
				ВыполнитьДействие() - асинхронная процедура команды, обращается к ВыполнитьДействиеНаСервере().
				ПолучитьОрганизациюПоУмолчанию() - возвращает организацию по умолчанию, срабатывает ПриОткрытии().
				ВыполнитьДействиеНаСервере() - процедура обращается к функции из модуля объекта. При успехе получает заполненный документ ОперацииБух.
				ПолучитьДатуОрганизациюОперацииБух() - возвращает Дату и Организацию ОперацииБух.
				