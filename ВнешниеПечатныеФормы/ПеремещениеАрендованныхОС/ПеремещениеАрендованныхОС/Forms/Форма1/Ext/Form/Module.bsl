
&НаСервере
Функция Команда1НаСервере()
	
	ТабДокумент = РеквизитФормыВЗначение("Объект");
	ТабДокумент = ТабДокумент.ПечатьОбъекта(Объект.СсылкаНаОбъект); 
	
	Возврат ТабДокумент; 
	
КонецФункции

&НаКлиенте
Процедура Команда1(Команда) 
	
	ТабДок = Команда1НаСервере(); 
	
	ТабДок.Показать();

КонецПроцедуры
