ОбщиеМодули:
	ЗагрузкаВыпискиПоБанковскомуСчету:
		Модуль:
				СоздатьДокументы() - процедура расширена блоком, который заполняет проект для текущего документа из Платежного Поручения. Именно через этот модуль обработка КлиентБанк создает документы.
				
Документ:
	СписаниеСРасчетногоСчета:
		Модули:
			МодульОбъекта:
				ОбработкаЗаполнения() - процедура расширена заполнением игсПроект из документа-основания ПлатежноеПоручение
				
	ПоступлениеНаРасчетныйСчет:
		Модули:
			МодульОбъекта:
				ПередЗаписью() - в конце процедуры вызывается процедура игсЗаполнитьПроект()
				игсЗаполнитьПроект() - новая процедура, которая подтягивает игсПроект из первого заполненного игсПроект у документа-сделки из ТЧ, если первый еще не заполнен
				ЗаполнитьПоДокументуОснованию() - заполняет новый реквизит документа по основанию
				
Подсистемы:
	игсМодифицированныеОбъектыМетаданных расширена:
		ОбщийМодуль.ЗагрузкаВыпискиПоБанковскомуСчету, Документ.СписаниеСРасчетногоСчета, Документ