Обработки:
	клЗагрузкаФактическихДанных:
		Реквизиты:
			УзелОбмена - расширен состав до ПланОбменаСсылка.клОбменУСО, ПланОбменаСсылка.игсБСОERPБюджетирование
		
		Формы:
			ЗагрузкаФактическихДанных:
				Изменения на самой форме:
					Реквизитный состав для ТаблицыЗначений "Мэппинг" расширен:
						"игсБСО" - новый реквизит, тип Булево, вынесен на форму, доступность для этой колонкиотключена по умолчанию
						
					Поле "ИнформационнаяБаза", наименование изменено "База УСО/БСО"
					
					Добавлено поле "Организация", путь: "Объект.Организация"
					
					Верхние поля разбиты на 2 группы (обе горизонтальные всегда): 
						"ГруппаШапка", состав: ИнформационнаяБаза, Организация, ГруппаКомандыФормы
						"ГруппаОтборы", состав: Период, ОтборСчетов, ОтборПроектов
						
				Изменения в модуле формы:
					1. УстановитьСоединение() - процедура расширена ветвлением в случае для БСО;
					2. ЗакрытьСоединение() - процедура закрывает и удаляет временный файл обработки в случае НЕ для БСО;
					3. ИнициализироватьФормуПолученияДанных() - процедура расширена ветвлением: для БСО открывается форма встроенного отчета, а для других узлов используется форма внешнего отчета;
					4. ПрочитатьФактическиеДанные() - процедура проверяет заполнена ли Организация (только для БСО) и заполняет данные для Отчета (расширено ветвлением как для БСО так и наоборот);
					5. ПолучитьТаблицуНастроекСтрокой() - функция расширена фильтрацией строк по признаку "игсБСО";
					6. ПриСозданииНаСервере - процедура расширена вызовом игсИзменитьДоступность();
					7. ИнформационнаяБазаПриИзменении() - процедура расширена вызовом игсИзменитьДоступность();
					8. ПолучитьСсылкуНаОрганизациюВСтороннейБазе() - функция теперь по-разному определяет Организацию (в завимости от текущего узла);
					9. ЗагрузитьНаСервере() - функция теперь по-разному определяет Организацию (в завимости от текущего узла);
					10. СохранитьМэппингНаСервере() - процедура сохраняет мэппинг с ориентиром на текущий  узел;
					11. УстановитьУзел() - процедура расширена ветвлением в случае для БСО;
					12. ТабСверткиСтатьяЗатратНачалоВыбора() - процедура расширена ветвлением: ChoiceMode только для узлов НЕ БСО;
					13. ЗаполнитьНП() - функция теперь по-разному определяет Организацию (в завимости от текущего узла);
					14. ЗагрузитьНПНаСервере() - функция теперь по-разному определяет Организацию (в завимости от текущего узла);
					15. ЗагрузитьРасчетыФОТНаСервере() - функция теперь по-разному определяет Организацию (в завимости от текущего узла);
					16. игсЭтоБСО() - новая функция, возвращает признак отношения текущего узла обмена к БСО;
					17. игсОрганизацияНеЗаполнена() - проверяет заполненость Организации на форме, выводит предупреждение, если Организация не заполнена;
					18. игсМэппингПриНачалеРедактирования() - устанавливает признак "игсБСО" в соответствии с текущим узлом;
					19. игсИзменитьДоступность() - меняет доступность некоторых полей/ТЧ и определяет ОтборСтрок в зависимости от узла.
					