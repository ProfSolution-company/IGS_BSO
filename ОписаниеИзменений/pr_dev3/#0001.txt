Документ:
    ПоступлениеТоваровУслуг:
		Реквизиты:
			"игсОбъектРаспределения" - Новый реквизит, составной тип данных "Справочники.игсБуровыеУстановки" и "Справочники.НоменклатурныеГруппы"
			"игсДатаНачала" - Новый реквизит, тип Дата
			"игсДатаОкончания" - Новый реквизит, тип Дата
			
		Модули:
			МодульОбъекта:
				ОбработкаПроверкиЗаполнения() - процедура расширена проверкой для ТЧ "Услуги"
				ОбработкаПроведения() - добавлено движение по регистру "игсЗатратыКРаспределениюПоПроизводственнойПрограмме"
			
			Модуль "ФормаДокументаОбщая":
				ОбработкаОповещения() - добавлен обработчик события для выбора ОР
				ПриОткрытии() - добавлен вызов процедуры игсОбновитьДоступностьСчет20()
				УслугиСчетЗатратПриИзменении() - добавлен вызов процедуры игсОбновитьДоступностьСчет20()
				УслугиСубконто1ПриИзменении() - добавлен вызов процедуры игсОбновитьДоступностьСчет20()
				игсОбновитьДоступностьСчет20() - новая процедура, меняет доступность столбцов ТЧ "Услуги" исходя из условий
				игсНастроитьФорму() - процедура расширена программным выводом на форму новых реквизитов ТЧ "Услуги"
				игсУслугиОбъектРаспределенияНачалоВыбора() - вызывает кастомный обработчик из общего модуля
				игсПроектЗакрыт() - серверная функция возвращает признак НГ, указанной в качестве аргумента 
			
			Модуль "ФормаДокументаУслуги":
				ОбработкаОповещения() - добавлен обработчик события для выбора ОР
				игсОбновитьДоступностьСчет20() - новая процедура, меняет доступность столбцов ТЧ "Услуги" исходя из условий
				игсНастроитьФорму() - процедура расширена программным выводом на форму новых реквизитов ТЧ "Услуги"
				игсУслугиОбъектРаспределенияНачалоВыбора() - вызывает кастомный обработчик из общего модуля
				игсПроектЗакрыт() - серверная функция возвращает признак НГ, указанной в качестве аргумента 

ОбщиеФормы:
	Модуль "РедактированиеСчетаЗатрат":
		ПриЗакрытии() - процедура расширена оповещением
		
Подсистемы:
   игсМодифицированныеОбъектыМетаданных - включены объекты: Документ.ПоступлениеТоваровУслуг, ОбщиеФормы.РедактированиеСчетаЗатрат